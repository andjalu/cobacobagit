#include <stdio.h>
int main()
{
    int a;
    printf("masukkan angka :");
    scanf("%d", &a);
    if (a >= 65)
    {
        printf("lulus");
    }
    else
    {
        printf("tidak lulus");
    }
    return 0;
}